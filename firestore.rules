rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid}/plannerState {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
